/* const usuarios = []

export const registrarUsuarios = (nombre, apellido, usuario, contrasenia) => {
  const nuevoUsuario = {
    id: usuarios[usuarios.length - 1]?.id + 1 || 1,
    nombre,
    apellido,
    usuario,
    contrasenia
  }

  usuarios.push(nuevoUsuario)
  console.log(usuarios)
} */

/*
5- Definir una función que muestre información sobre una cadena de texto que se le pasa como argumento. A partir de la cadena que se le pasa, la función determina si esa cadena está formada sólo por mayúsculas, sólo por minúsculas o por una mezcla de ambas.

*/

//RegExp()

/* const validacionMinusculas = new RegExp(/^[a-z]+$/).test("HOLa")
const validacionMayusculas = new RegExp(/^[A-Z]+$/).test("HoLA")


if (validacionMinusculas) {
  console.log("el texto esta en minusculas")
} else if (validacionMayusculas) {
  console.log("el texto esta en mayusculas")
} else {
  console.log("el texto esta en una combinacion de mayusculas y minusculas")
}
 */

/* tarea array=[productos] */

//como generar un buscador en tiempo real

//celular
